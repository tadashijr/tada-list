name: Deploy TADA-List

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify secrets are set
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
        run: |
          if [ -z "$FIREBASE_API_KEY" ]; then
            echo "❌ ERRO: FIREBASE_API_KEY não está configurada nos secrets do repositório."
            echo "Acesse: Settings → Secrets and variables → Actions → New repository secret"
            exit 1
          fi
          echo "✅ Secrets detectadas, prosseguindo com o deploy."

      - name: Inject Firebase secrets into HTML
        env:
          FIREBASE_API_KEY:             ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN:         ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID:          ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET:      ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID:              ${{ secrets.FIREBASE_APP_ID }}
          FIREBASE_MEASUREMENT_ID:      ${{ secrets.FIREBASE_MEASUREMENT_ID }}
        run: |
          python3 - <<'EOF'
          import os, re

          with open("tada-list.html", "r", encoding="utf-8") as f:
              html = f.read()

          replacements = {
              "__FIREBASE_API_KEY__":             os.environ["FIREBASE_API_KEY"],
              "__FIREBASE_AUTH_DOMAIN__":         os.environ["FIREBASE_AUTH_DOMAIN"],
              "__FIREBASE_PROJECT_ID__":          os.environ["FIREBASE_PROJECT_ID"],
              "__FIREBASE_STORAGE_BUCKET__":      os.environ["FIREBASE_STORAGE_BUCKET"],
              "__FIREBASE_MESSAGING_SENDER_ID__": os.environ["FIREBASE_MESSAGING_SENDER_ID"],
              "__FIREBASE_APP_ID__":              os.environ["FIREBASE_APP_ID"],
              "__FIREBASE_MEASUREMENT_ID__":      os.environ.get("FIREBASE_MEASUREMENT_ID", ""),
          }

          for placeholder, value in replacements.items():
              if placeholder in html:
                  html = html.replace(placeholder, value)
                  print(f"✅ Substituído: {placeholder}")
              else:
                  print(f"⚠️  Não encontrado: {placeholder}")

          with open("tada-list.html", "w", encoding="utf-8") as f:
              f.write(html)

          print("✅ tada-list.html atualizado com sucesso.")
          EOF

      - name: Confirm substitution worked
        run: |
          if grep -q "__FIREBASE_API_KEY__" tada-list.html; then
            echo "❌ ERRO: Placeholder ainda presente no HTML. Verifique os secrets."
            exit 1
          fi
          echo "✅ Todos os placeholders foram substituídos."

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
